name: ZeroTwo / system
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/zerotwo-system.yml
      - zerotwo/system/**
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: terraform init
        uses: ./.github/actions/terraform-local-action
        with:
          host: aqua.sapslaj.xyz
          dir: zerotwo/system
          cmd: init
          tfe_token: ${{ secrets.TFE_TOKEN }}
      - name: terraform plan
        uses: ./.github/actions/terraform-local-action
        with:
          host: aqua.sapslaj.xyz
          dir: zerotwo/system
          cmd: plan
          tfe_token: ${{ secrets.TFE_TOKEN }}
      - name: terraform apply
        if: github.ref == 'refs/heads/main'
        uses: ./.github/actions/terraform-local-action
        with:
          host: aqua.sapslaj.xyz
          dir: zerotwo/system
          cmd: apply -auto-approve
          tfe_token: ${{ secrets.TFE_TOKEN }}
