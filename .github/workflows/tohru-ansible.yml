name: Tohru / Ansible
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/tohru-ansible.yml
      - tohru/ansible/**
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Run ansible-playbook
        uses: ./.github/actions/run-playbook
        with:
          dir: tohru/ansible
          inventory: tohru.sapslaj.xyz,
          vault_password: ${{ secrets.vault_password }}
